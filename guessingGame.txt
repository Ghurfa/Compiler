myNamespace namespace
{
    Program class
    {
        myInt := 2: public static 
        myDouble:double

        ctor(myFloat:float)
        {
            myDouble = (double)myFloat;
        }

        Average static int(a:int, b:int)
        {
            return ((a + b++) / 2);
        }

        Main static void(args:string[])
        {
            number = Random.RandInt[0, 64] : int
            guess:int = -1,
            
            end:var = false,
            while(!end)
            {
                while (guess != number)
                {
                    guess = Console.ReadLine(),
                    if (guess < number)
                    {
                        Console.PrintLine("Too low"),
                        if (Average(guess, number) - guess > 10) Console.PrintLine("Very off");
                    } 
                    else if(guess > number) 
                    {
                       Console.PrintLine("Too high")
                    }
                    else
                    {
                        Console.Println("Correct"),
                        if (guess != number) throw new NotImplementedException();
                    }
                }
                Console.Println("Continue? (y/n)"),
                option:var = Console.ReadLine(),
                end = option == "y" || option == "n",
            }
        }
    }
}