FizzBuzz namespace
{
    FizzBuzz class
    {
        FizzBuzz public static void(start:int, end:int)
        {
            c:(a:int, b:int) = (3, 4)
            for(i := start, i < end, i++)
            {
                i = 1
                fizz := i % 3 == 0
                buzz := i % 5 == 0;
                print:string = fizz && buzz ? "FizzBuzz" : fizz ? "Fizz" : buzz ? "Buzz" : i.ToString[];

            }
        }
    }
}